{
   "@type" : "EnvironmentExtensions",
   "connections" : {
      "@type" : "${extensionType}",
      "connection" : [
         {
            "@type" : "${extensionType}",
            "field" : [
               {
                  "@type" : "Field",
                  "id" : "password",
                  "value" : "${fieldVal}",
                  "encryptedValueSet": "true",
                  "usesEncryption" : "true",
                  "componentOverride" : "false",
                  "useDefault" : "false"
               }
               ],
               "id": "${extensionId}"
         }
         ]
   },
   "environmentId": "${environmentId}",
   "id": "${environmentId}",
   "partial": "true"
}
