{
   "@type" : "EnvironmentExtensions",
   "connections" : {
      "@type" : "${componentType}",
      "connection" : [
         {
            "@type" : "${componentType}",
            "field" : [
               {
                  "@type" : "Field",
                  "id" : "password",
                  "value" : "${fieldVal}",
                  "encryptedValueSet": "true",
                  "usesEncryption" : "true",
                  "componentOverride" : "false",
                  "useDefault" : "false"
               }
               ],
               "id": "${componentId}"
         }
         ]
   },
   "environmentId": "${environmentId}",
   "id": "${environmentId}",
   "partial": "true"
}
